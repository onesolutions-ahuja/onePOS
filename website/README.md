# onePOS Marketing Website

This directory owns the public onePOS marketing website (`onepos.com`).

The current marketing React implementation is still imported from `src/marketing` during this structure-only migration so runtime behaviour is unchanged. It can be extracted into an independent bundle later without mixing that change with the directory migration.
