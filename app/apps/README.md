# Installable Apps

Each app/module has one canonical implementation in this directory. Example future module: `hrms/`.

A tenant installation is represented by package/module metadata, licence/entitlement and permissions in the database. Do not create `clients/<tenant>/...` source copies.
